/*리스트, 맵 자료형의 차이*/
공통점
- 데이터를 저장하는 자료구조

LIST
- 순차적으로 데이터를 저장하거나 접근할 때 사용
- 메모리안의 특정한 동일공간에 뭉텅이로 저장되기 때문에
  알맞는 데이터 저장공간만 있다면 데이터 저장속도가 빠름

MAP
- '키-값'의 쌍으로 나열되어 있어 키값을 통해 원하는 값을 쉽게 찾을 수 있다.
- 빈공간을 찾아서 저장하기 때문에 데이터 저장속도가 느릴 수 있다.
- 빈번한 검색과, 범위데이터가 아닌 특정 데이터를 순간마다 캐치해야할 때 유리한 자료구조

/*링크드리스트, 배열리스트의 차이*/
공통점
- 둘다 리스트이다

링크드리스트
- 논리적순서에 따라 순차적으로 데이터입력. 물리적주소는 순차적이지 않음
- 인덱스를 안가지고 현재위치의 이전 및 다음위치를 기억함. 데이터에 접근성이 느림.
- 삽입/삭제에 용이. 논리적주소만 바꿔주면 되기 때문.

배열리스트
- 논리적순서에 따라 순차적으로 데이터입력. 물리적주소 또한 순차적
- 인덱스를 가지고 있어서 데이터 접근성이 빠름
- 삽입/삭제에 취약. 삽입/삭제가 이루어진 위치의 다음부터 모든 데이터의 위치가 변경되야함.